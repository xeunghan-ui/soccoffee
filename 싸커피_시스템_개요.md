# 싸커피 시스템 개요 — 작업 전 필독 (2026-08-16 기준)

무언가를 바꾸기 전에 이 문서로 전체 구조를 파악한다. 상세 규칙은 각 스펙 문서(활동정원제_개편_스펙.md, 팀리그_모드_스펙.md, Supabase_전체설정.md, 브랜드_컬러.md)에 있고, 여기는 "무엇이 어디에 있고 서로 어떻게 묶이는지"의 지도다.

## 1. 구성

- **소개(index.html + intro.js/css)** — 공개 랜딩. 로그인 없음. 일정·스쿼드를 읽기전용 표시. SEO/FAQ 스키마 포함.
- **멤버(member.html + member.js/css)** — 회원 앱. 이름+PIN 로그인, 로그인 상태는 기기에 유지. 홈/일정/멤버/더보기 하단 탭 + 운영진 콘솔.
- **팀빌더(team/)** — 명단 관리 도구. 선수·등번호·성별·휴면월·활동예외월의 **원본**.
- **발송기(scripts/send-push.mjs)** — GitHub Actions로 매일 여러 번 실행. 푸시 발송 + 날짜 기반 자동화(세션 자동 등록·정원 롤오버·최종 확정). VAPID 키는 GitHub Secrets에만.

## 2. 데이터 (Supabase)

- `club_settings` — id별 jsonb 문서:
  - `current`: sessions, notices, dues 부가(duesConfirmed), pins, bank, league, capacity(result), uniform, guestReqs, pushTemplates …
  - `teambuilder`: players[] (명단 원본 — status, gender, dormantMonths, activeMonths, jersey…)
  - `push_state`: 발송기 중복 방지 상태(sent[] once 가드)
- 행 단위 테이블(멤버별 반복 쓰기): `dues`(month,member_id,paid), `cap_confirm`(month,member_id,state — at은 서버 now()), `bell_reads`, `push_subs`, `attendance`, votes.
- **철칙**: 여러 멤버가 각자 쓰는 값을 current 안 중첩 객체에 두지 않는다(행 테이블로). 통째 객체 저장이 낳은 사고: 자리 확인 유실(→cap_confirm 분리), PIN 유실(→savePinPatch 병합), 알람 리셋(→bell_reads). pins·league·duesConfirmed처럼 current에 남은 것은 반드시 **최신 재조회 후 병합 저장**.

## 3. 역할

- 총괄(ADMIN_NAMES=박승한): 전체 편집, 운영진 콘솔 7탭(공지·세션·회비·투표·푸시·리그·설정), 할 일 대시보드(더보기 상단에도 노출).
- 일반 관리자(SUB_ADMIN 4명): 회비 현황 열람, 감독 지원·추천 이름 열람.
- 총무(DUES_CONFIRM_NAMES): 회비판 입금확인 버튼.
- 회원 / 친구(friends: 명단·회비 제외) / 탈퇴(former: 모든 집계 제외).

## 4. 월간 사이클과 정원제 (요약 — 상세는 CLAUDE.md 정책 달력·정원제 스펙)

15일 신청 창 → 25일 유지 마감(확인+납부+**입금확인**까지 = 정원 포함·참석 가능·팀뽑기 명단) → 26일 롤오버(남은 자리를 복귀 **순번**대로 배정, 이후 복귀자 입금~말일) → 익월 5일 이후 최종 확정. 정원 남24·여12. 확정 결과(result)가 모든 활동/휴면 판정의 최우선 근거.

**설계 철학**: 회비는 회원이 직접 '납부 표시'(자가신고 학습) → 총무 입금확인이 정본. 시스템이 대신 눌러주지 않는다. 입금확인 없이는 정원·참석·팀뽑기·감독추천 어디에도 안 들어간다.

## 5. 리그 모드 (3·5·9·11월)

- current.league[월] = { white, black, captains, applicants, recs, match } — 멤버 앱 소유(팀빌더 미접촉).
- 감독: 지원(15일~, 홈 카드) → 2명 초과면 총괄이 지명. 지원자 0명이면 **추천제**(입금확인 완료 회원만, 1인 1추천, 관리자에게만 이름 노출).
- 팀 뽑기(드래프트): 명단 = 확정 회원. 세션 시간 20:00(일반 달 21:00).

## 6. 푸시

- 구독(push_subs)·읽음(bell_reads)·설정 모두 **계정(member_id) 기준** — 기기 기준 아님.
- 발송기 자동 발송: 15일 활동확인·복귀 안내, 24일 자리확정 재촉(유지자 입금확인 기준, 복귀자 제외), 25일 투표 시작, 마감 임박·내일 세션 리마인드, 공지·입금확인 개인 알림.
- push_state 저장 실패 시 exit(1) — 조용한 실패 금지(과거 투표 알람 4연발의 원인).

## 7. UI 원칙

- 문구는 간결체(해요체 지양), 이모지 없음, 상태별로 "다음 행동 하나"만 안내.
- 색은 브랜드_컬러.md 토큰만(새 hex 금지). css/js 수정 시 HTML의 ?v= 반드시 범프.
- 전체 현황(정원 카운트·추천인 등)은 운영진 화면에만, 회원에겐 자기 상태·순번만.

## 8. 작업 수칙

- 바꾸기 전: 이 문서 + CLAUDE.md 정책 달력 + 관련 스펙 확인. 화면의 모든 숫자는 "누가·언제 봐야 맞나"를 대조.
- 데이터 쓰기: 병합 저장 원칙. 명단·성별 등 원본은 팀빌더.
- 멀티 세션 git: 자기 커밋만, pull --rebase, force push 금지.
