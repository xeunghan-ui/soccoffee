# 싸커피 브랜드 컬러 가이드

현재 코드 전체에 고유 색상값 91개가 흩어져 있다(2026-07 조사). 아래 토큰이 기준이며, **새 색상값을 추가하지 말고 이 표에서 고른다.** 기존 유사색은 수정할 때마다 점진적으로 토큰으로 수렴시킨다.

## 코어 팔레트 (기준값)

| 역할 | 토큰 | 값 | 비고 |
|---|---|---|---|
| 배경(메인 그린) | --bg / --green | `#15281B` | 사이트 전체 기준색. ~~#14281b~~ 는 이 값으로 통일함 |
| 카드 배경 | --card | `#1E3526` | |
| 카드 안 상자·경계 강조 | | `#2A3D30` | |
| 구분선 | --line | `#33492F` | |
| 본문 텍스트(크림) | --cream / --coffee | `#ECE6D2` | |
| 밝은 강조 텍스트 | | `#F3EED9` | 소개 헤딩 전용 |
| 보조 텍스트(크림 딤) | --coffee-2 | `#D6CFB4` | |
| 포인트(샌드 골드) | --accent / --gold | `#DDD6B6` | 버튼·강조 배경 |
| 흐린 텍스트 | --muted | `#A4B39A` | 유사색(#95a083 #8c9a7c #8a9a7e #9fb0a3 #7a8a6c)은 2026-07-29 `#A4B39A`로 통일 완료 |
| 긍정/성공 | --green(멤버앱) | `#46B381` | 짙은 배경용은 `#5A9277` |
| 경고/오류 | --red / --alert | `#DC5238` | 옅은 톤 `#BD6452` |
| 골드 브라이트(인쇄·제안서) | | `#E0A530` | 대외 문서 포인트 전용 — 웹에서는 쓰지 않음 |

## 규칙

1. **CSS에서는 반드시 var() 토큰을 쓴다.** 하드코딩 hex 추가 금지.
2. JS 인라인 스타일도 가능한 한 var(--cream) 형태로. 불가피하면 위 표의 값 그대로.
3. 팀빌더(team/)는 라이트 테마로 별도 체계 — 이 표의 다크 팔레트를 섞지 않는다.
4. 대외 문서(제안서 등)는 배경 #15281B · 크림 #ECE6D2 · 골드 #E0A530 조합.
5. 남은 유사색 정리는 화면 단위로 수정할 일이 있을 때 함께 수렴시킨다(일괄 치환은 회귀 위험).
