# 시행착오 노트 — 총괄 × 싸커피(Claude) 협업 사례집

같이 일하며 실제로 겪은 삽질·오판·사고와, 거기서 굳어진 일하는 방식. 새 세션의 Claude와 총괄 모두 이 패턴을 전제로 움직인다. (규칙의 원문은 `CLAUDE.md`, 시스템 지도는 `싸커피_시스템_개요.md`)

## 1. 데이터를 단정하기 전에 원본을 다시 본다

- **장세영 미응답 오판** — Claude가 "장세영 미응답"이라고 보고했는데, 실제로는 본인이 8/23에 '휴면'으로 응답한 상태였다. 조회 쿼리가 `state='active'`만 필터해서 응답 자체가 안 보였던 것. → **집계를 보고할 때는 필터 조건을 같이 말하고, "미응답/없음" 같은 부정 단정은 전체 데이터로 재확인한다.**
- **재유·솔·지원 일정 미응답(2026-08-31)** — "투표가 안 되는 버그인가?"로 시작했지만, 데이터를 까보니 자격·기능 문제가 아니라 **응답 마감(8/30)이 지난 것**이었다. → 버그 제보가 와도 코드보다 **데이터(마감·자격·기록)를 먼저 조회**하면 헛수고를 줄인다.

## 2. 추측 수정 금지 — 원인을 코드로 따라간다

- **팀빌더 신규 3명 미표시(장기 미스터리)** — 티어 문제, 캐시 문제 등 추측을 여러 번 하다 오래 헤맸다. 진짜 원인은 연도 필터 `inPoolForYear`가 **미래 가입일 회원을 탈락**시키던 것 한 줄. → 증상이 이상하면 추측으로 고치지 말고 **해당 값이 만들어지는 경로를 끝까지 추적**한다.
- **남성 정원 35명 표시** — 원인은 ROSTER에 gender 필드가 없어서 전원 '남' 기본값이 된 것. 표시 로직이 아니라 **데이터 필드 누락**이 원인인 경우가 많다.

## 3. 이름·용어는 정확 일치로

- **싸커피 데이 뱃지 전원 획득 사고** — '데이' 부분일치로 검사해서 "매치데이" 세션까지 전부 매칭됐다. → **'싸커피데이' 정확 일치(공백 제거)**로 수정. 팀빌더에 세션 넣을 때도 명칭을 정확히 '싸커피 데이'로 적어야 뱃지가 자동 반영된다.
- **카풀 드라이버 닉네임** — '방판맨'=최승호, '카쩨쥔장'=한승재처럼 닉네임 등록이 있어 `DRIVER_ALIAS` 매핑을 둠. 새 닉네임이 보이면 매핑에 추가해야 집계에 잡힌다.
- **포지션 옛 용어** — 풋살 용어(픽소 등)를 일반 용어(수비수)로 바꾼 뒤에도, 옛 저장값을 변환 없이 보여주는 화면이 하나 남아 있었다(홈 프로필 칩). → **용어를 바꾸면 그 값을 표시하는 모든 화면을 grep으로 찾는다.**

## 4. 탈퇴·재가입·휴면 — 사람 이력은 시트가 원본

- **함께한 기간 과대 계산** — 이일웅·박우성·박광우·심지수처럼 탈퇴 후 재가입한 회원의 기간이 공백 없이 계산됐다. 총괄 지적 → 탈퇴~재가입 공백 차감 로직 추가.
- **심지수 이력·2025년 초 세션 복원** — DB에 없던 출석 이력을 **출석 시트(스프레드시트) 기준**으로 복원했고, 중복 세션 4개도 시트 대조로 삭제했다. → **이력 데이터가 어긋나면 시트가 원본**. 그리고 "탈퇴자는 미리 넣지 않는다 — 재가입할 때 그때 입력"(2026-08 총괄 결정).

## 5. 공유 데이터 덮어쓰기 — 이 저장소 최대 사고 유형

- **유니폼 확정이 계속 지워지던 사건** — 로드 시점 캐시로 `current` 전체를 upsert해서 그 사이 남이 바꾼 값을 리셋. → `saveSettings`는 **저장 직전 재조회 후 병합**으로 수정됨.
- **정원 자리 확인이 조용히 사라진 사건** — 여러 멤버가 동시에 쓰는 값을 `capacity.confirm`(중첩 객체)에 넣었더니 15일 알림 직후 동시 신청이 서로를 덮었다. → **여러 명이 각자 쓰는 데이터는 행 단위 별도 테이블로**(`cap_confirm` 분리).
- **루트 index.html이 팀빌더로 덮인 사고 / force push로 남의 커밋 유실** — `git add -A` 금지, push 전 `git pull --rebase`, force push 절대 금지 규칙이 여기서 나왔다.

## 6. 모바일은 다른 세상이다

- **김유솔 아이폰 로그인 공백** — 데스크탑은 되는데 모바일(카카오 인앱)만 이름 목록이 안 떴다. 원인은 jsDelivr CDN의 supabase-js 로드 실패 → 스크립트 전체 사망. → **supabase-js를 저장소에 셀프호스팅**(`js/supabase.js`)하고 초기화는 try/catch. 외부 CDN 의존은 모바일·인앱에서 언제든 깨질 수 있다는 교훈.
- 모바일엔 뒤로가기가 없어 이동이 막히는 문제 → 하단 왼편 플로팅 ← 버튼. UI 제안은 총괄이 위치·형태를 여러 번 뒤집을 수 있으니(하단 바 → 플로팅) **작게 만들어 보여주고 고치는 게 빠르다.**

## 7. 디자인은 시안 → 피드백 반복이 기본

- **뱃지 8종** — 원 밖으로 새는 요소(야유회 모래, 태양 광선), "태양 못생김", 톤앤매너 불일치까지 여러 라운드를 돌았다. 최종적으로 **전역 원 클리핑**(`.bsvg { clip-path: circle(50%) }`)과 톤 통일 재작업으로 수렴. → 디자인 요청은 한 번에 완성이 아니라 **미리보기 → 지적 → 수정 루프**를 전제한다.
- **드립백 목업** — 이미지 실제 픽셀 크기와 표시 배율을 혼동해 크롭이 어긋났고, 핑크 재단선이 남았다. 레퍼런스도 중간에 교체됨(HAMAYA → 르무통). → 이미지 작업은 **실측 크기 확인 + 결과물 이미지로 검증** 후 전달.

## 8. GIPHY 스티커 — 요건을 하나씩 확인하며 뚫었다

업로드 거부 원인을 세 단계에 걸쳐 규명했다: ① 정지 GIF는 스티커로 거부(**애니메이션 필수**) → ② 속이 빈 흑백 라인 그림 거부(**내부 채움 + 바깥 투명 유지**, 첫 프레임 투명도 20% 이상) → ③ 채움 후 그림별 채색(촛불 불꽃 주황 등). 통과 후에도 "패턴이 다 비슷하다"는 피드백으로 **이미지별 맞춤 모션**(유니폼 펄럭임·파이어볼 비행·별 트윙클 등)으로 재작업. 투명도 20% 딱 걸린 파일은 캔버스 패딩으로 여유를 확보했다. → 외부 플랫폼 업로드는 **거부 사유를 가설로 세워 하나씩 검증**한다.

## 9. 하나를 고치면 연쇄된 곳까지

- **안재영 회비 환불** — 입금확인 명단(`duesConfirmed`)만 고치면 회비판(`dues` 테이블)과 어긋난다. 두 군데를 같이 수정해야 화면이 일관된다.
- **리그 20시 → 21시 통일** — 배너 하나가 아니라 세션 폼 기본값·발송기(send-push.mjs)·FAQ·소개 페이지·CLAUDE.md까지 **grep으로 전부 찾아 한 번에** 바꿨다. 시간·정원·명칭 같은 정책값 변경은 항상 전체 검색.
- **월 경계 함수 3종 구분** — `nowMonthStr()`(달력), `statusMonth()`(15일부터 다음 달: 회비·자리확인·감독 카드), `squadMonth()`(마지막 수요일 지나면 다음 달: 팀 현황·시즌 배너). 시즌 배너가 8/31에 "일반 경기"로 떴던 버그는 이 기준을 잘못 고른 것. **새 화면을 만들 땐 셋 중 어느 기준월인지 먼저 정한다.**

## 10. 결정은 총괄, 실행은 Claude — 그리고 기록

- 동률 처리(박원주 vs 심소른 3:3)처럼 판단이 갈리는 건 Claude가 **집계 + 선택지**로 정리해서 묻고, 총괄이 고른다(→ 결선 투표로 결정).
- "금일 6시"처럼 해석 여지가 있으면 Claude가 **해석을 명시하고 진행**한다(오후 6시로 확인). 마감시각·기준은 처음부터 명시하면 왕복이 준다.
- 확정된 규칙(신규 입금 26일~말일, 리그 21시, 드라이버 뱃지 5회, 겨울 뱃지=전년 12월+1월…)은 **그 자리에서 CLAUDE.md에 기록**한다. 세션이 길어지면 요약으로 이어지므로 기록 안 된 결정은 잊힌다.
- 커밋까지는 Claude가 하지만 **푸시는 항상 총괄**. Claude는 응답 끝에 푸시 대기 개수를 알려준다.
