# 싸커피 인수인계 — 현재 상황 (2026-07-28)

> 새 코워크 세션이 바로 이어받기 위한 현황 스냅샷.
> **작업 규칙·구조는 `CLAUDE.md`가 원본**(반드시 먼저 읽기). 배경은 `인수인계.md`, 소개 페이지는 `소개페이지_인수인계.md`, 팀빌더는 `팀빌더_인수인계.md`.

---

## 0. 30초 요약

- 정적 사이트 + Supabase(anon 키). GitHub Pages 배포(`git push`). **푸시 직접 못 함 → 커밋 후 사용자에게 안내**.
- 멤버앱 = `member.html`(셸) + `css/member.css` + `js/member.js`. 소개 = `index.html` + `css/intro.css` + `js/intro.js`.
- **CSS/JS 수정 시 `member.html`의 `?v=` 캐시버스터 올리기.** JS는 커밋 전 `node --check js/member.js`.
- **`git add -A` 금지**(변경 파일만). 루트 `index.html`은 소개(팀빌더로 덮지 말 것). 푸시 전 `git pull --rebase`, `git push -f` 금지.
- **여러 코워크 동시 편집** — 파일 쓰기 전 `git log`/`git status` 확인, 자기 커밋만 rebase 후 푸시.

## 1. 현재 버전 (member.html)

- `css/member.css?v=20260724i`
- `js/member.js?v=20260728b`
- 소개: `intro.css?v=20260727a`, `intro.js?v=20260722b`
- ※ CSS/JS 고칠 때마다 이 숫자 올리기(안 올리면 CDN 캐시로 최신본 안 보임).

## 2. 최근에 들어간 것 (이번 사이클)

- **활동 정원제 1단계 구현됨**(커밋 2939dde) — 남24·여12 성별 분리, 월별 재확인(무응답=휴면), 선착순 복귀, 2단계 입금 판정. 스펙: `활동정원제_개편_스펙.md`(§9에 총괄 구현 결정 기록).
- **팀 리그 모드 구현됨**(커밋 78c4b28) — 팀 배정(운영진 수정 가능)·감독 지원/지명·참석 팀별 카운트·배너·매치 기록(월 1매치, 승률·순위 없음). 스펙: `팀리그_모드_스펙.md`.
- 홈 투표 카드(투표 기간 표시), 하루종일/다일(1박2일) 세션 표시, 게스트 노출 정리(참석 필터에서만·필터 테두리), 일정 탭 배지(미응답+미정), 회비 용어 통일(납부/입금확인), 프로필(포지션 풋살명·강점3/약점3·카드 정리), 하단 admin-bar 제거(로그아웃=더보기), 알림 종 아이콘 등 다수 UI 수정.

## 3. 지금 확인/이어서 할 것 (TODO)

- **⚠️ `cap_confirm` 테이블 SQL 실행 (9월 정원제 시작 전 필수)** — `Supabase_전체설정.md`의 "cap_confirm" 절을 SQL Editor에서 Run. **안 돌리면 8월 15일 자리 확인 버튼이 저장 실패**한다. (자리 확인을 `capacity.confirm` 한 덩어리에 넣었더니 동시 신청이 서로를 덮어써 신청이 조용히 사라지는 문제가 있어 멤버별 행 테이블로 분리 — 2026-07-29.)
- **⚠️ 성별 데이터 확인 (8월 15일 전)** — `capGender`는 `팀빌더 players.gender → GENDER_FIX → '남'` 순. 팀빌더에 성별이 빈 여성 회원이 있으면 **조용히 '남'으로 세어져** 여 12는 미달, 남 24는 초과로 계산된다. 현재 보정은 `GENDER_FIX={'심지수':'여'}` 하나뿐. 여성 13명 전원 `gender` 값 확인 필요(`ROSTER_SEED`엔 gender 필드가 없어 팀빌더가 유일한 출처).
- **정원제 실동작 검증** — 8월 15일 이후 실제 흐름(활동 확인·복귀 선착순·26일 잠정확정·입금확인 최종확정) 라이브 점검. 발송기(`scripts/send-push.mjs`) 26일·5일 처리 확인. 특히 **5일 최종 확정은 그 달 `duesConfirmed`가 1건이라도 있어야 실행**(안전장치) — 총무가 5~14일에 입금확인을 안 하면 잠정 상태로 남는다.
- **팀 리그 — 팀빌더 쪽 중복 UI 정리** — 역할 분담은 `팀리그_모드_스펙.md §9`에서 **(a) 멤버앱 처리로 확정**됨(감독 지원·지명·팀 배정·매치 결과 전부 멤버앱, 저장은 `current.league`). 남은 일은 팀빌더에 같은 기능이 남아 있으면 정리하고 "결과는 멤버앱에서 입력"으로 안내 바꾸기.
- **팀빌더 동기화 리스크** — 명단·휴면 원본은 팀빌더. 사이트가 쓴 `activeMonths`를 팀빌더 저장이 덮을 수 있음(미해결). 정원제 데이터 자체는 팀빌더 row를 안 씀(자리 확인=`cap_confirm` 테이블, 확정 결과=`current.capacity`). 다만 `capSetNext`가 확정 후 `setMyDormancy`로 팀빌더 `activeMonths`도 갱신하므로 **그 경로는 여전히 덮어쓰기 대상**.
- **명단 상태 불일치** — `teambuilder.players`와 `current.roster` 두 곳의 status가 일부 어긋남(예: 장세영·김재유·조수연·정은용). 어느 쪽이 원본인지(앱은 팀빌더 기준) 정리 필요.
- **하단 네비 "떠 보임"** — 코드상 `position:fixed; bottom:0`로 정상 고정. 특정 PWA에서 떠 보이면 스크린샷 확보 후 재현 필요(미해결 가능성).
- **신규·대기 회원** — 정원제 §8 '추후 단계'. 현재 팀빌더/수동.

## 4. 데이터 구조 요점 (Supabase `club_settings` jsonb)

- `id='current'` — 설정 한 덩어리: `sessions`, `pins`, `bank`, `uniform`, `profiles`, `duesConfirmed`, `guestReqs`, `guestExtra`, **`capacity`(정원제 확정 결과 `{월:{result:{active[],finalized}}}`)**, **`league`(팀 리그 `{월:{white[],black[],captains[],applicants[],match}}`)**. **저장 = 최신값 재조회 후 병합(patch)** 필수(`saveSettings`). 통째 upsert 금지.
- **⚠️ 그 병합은 최상위 키 단위 얕은 병합**(`{...cur, ...patch}`)이라 **같은 키 안의 남의 변경은 못 지킨다.** 여러 멤버가 각자 쓰는 데이터는 `current`에 넣지 말고 행 단위 PK 테이블로 뺄 것.
- `cap_confirm` **(별도 테이블)** — 정원제 자리 확인. PK `(month, member_id)`, `state`('active'|'dormant'), `at`(선착순 기준, 서버 시각 — 상태가 바뀔 때만 트리거로 갱신). 위 얕은 병합 문제 때문에 `capacity.confirm`에서 분리(2026-07-29). 레거시 blob은 읽기 폴백만 남아 있음.
- `id='teambuilder'` — 명단(`players`: gender·status·dormantMonths·activeMonths…), 매치, 팀히스토리. **팀빌더(별도 앱/코웤)가 전체 덮어씀** → 동시성 last-write-wins.
- 그 외 테이블: `notices`, `attendance`, `dues`, `potm_votes`, `rides`, `push_subs`, `push_queue`, `bell_reads`.
- 정원제 상수(`js/member.js`·`scripts/send-push.mjs` 양쪽 동일하게 유지): `CAP_START='2026-09'`(이 달분부터 적용 → 신청 창은 8/15 열림), `CAP_LIMIT={남:24, 여:12}`, `GENDER_FIX={'심지수':'여'}`.

## 5. 권한 · 핵심 규칙

- 총괄관리자 `ADMIN_NAMES=['박승한']`(전체 편집). 일반관리자 `SUB_ADMIN_NAMES=['원재식','홍순인','최승호','정희범']`(회비 현황 읽기전용). 입금확인 `DUES_CONFIRM_NAMES=['박승한','원재식']`.
- 로그인 = 이름 + PIN 4자리(SHA-256, `current.pins`).
- 회비/활동 판정 = 매월 15일부터 다음 달(`duesMonth()`=`statusMonth()`). 휴면 = 회비·참석·명단·투표 제외. `activeMonths` 예외 우선(`isDormantFor`).
- 이미지 추가 전 PIL 압축(1920px·q82·exif_transpose).

## 6. 참고 문서

- `CLAUDE.md` — 작업 규칙(원본, 최우선)
- `활동정원제_개편_스펙.md` — 정원제 정책·구현·§9 결정
- `팀리그_모드_스펙.md` — 팀 리그 규칙·기능·§7 미결
- `Supabase_전체설정.md` / `공지_참석_회비_Supabase_설정.md` — 테이블 SQL
- `웹푸시_설정.md` / `scripts/send-push.mjs` — 웹푸시(GH Actions)
- `팀빌더_인수인계.md` / `소개페이지_인수인계.md`

## 7. 배포 절차 (커밋 후 항상 안내)

```
cd ~/Documents/Claude/Projects/socoffee
find .git -name '*.lock' -delete
git add <변경 파일만>
git commit -m "..."
git pull --rebase origin main
git push origin main
```
